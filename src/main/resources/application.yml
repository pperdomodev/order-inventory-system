server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_inventory
    username: root
    password: B@tman2025*

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  data:
    redis:
      host: localhost
      port: 6379

  jackson:
    serialization:
      indent_output: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: myVeryStrongSecretKeyForJwtAuthentication2026SecureKey
  expiration: 3600000